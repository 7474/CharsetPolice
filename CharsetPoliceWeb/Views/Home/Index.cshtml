@model CharsetPoliceWeb.Models.InvestigationRequest
@{
    ViewData["Title"] = "Top";
}

<h2>Charset Police</h2>

<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" asp-controller="Investigation">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Uri" class="control-label"></label>
                <input asp-for="Uri" class="form-control" />
                <span asp-validation-for="Uri" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="通報する" class="btn btn-default" />
            </div>
        </form>
        <div class="alert alert-info" role="alert">
            <p>指定したURLの文字エンコーディング指定が妥当かどうかを捜査します。</p>
            <p><strong>「文字コード警察だ！」</strong></p>
        </div>
    </div>
</div>